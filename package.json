{
  "name": "queryrecords",
  "version": "0.1.0",
  "private": true,
  "description": "QueryRecords — music catalog site for Zag Query artists (albums, EPs, singles by artist).",
  "scripts": {
    "dev": "http-server . -p 8000 -c-1 --silent",
    "start": "http-server . -p 8000 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
